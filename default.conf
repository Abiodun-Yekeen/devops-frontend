# server {
#     listen       80;
#     listen  [::]:80;
#     server_name  51.20.34.227;

#     # Serve React.js frontend for root path and specific routes
#     location / {
#         root /usr/share/nginx/html;
#         index index.html;
#         try_files $uri  /index.html;
#     }

#     # Proxy requests starting with /api/ to Node.js backend
#     location /api/ {
#         rewrite ^/api(/.*)$ $1 break;  # Remove the '/api' prefix
#         proxy_pass http://13.48.48.16:8085;   
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#     }

#     # Handle other requests or static files
#     # location /static {
#     #     # Configure static file serving settings if needed
#     #     root /usr/share/nginx/html/;
#     # }

#     error_page   500 502 503 504  /50x.html;
#     location = /50x.html {
#         root   /usr/share/nginx/html;
#     }
# }

server {
    listen       80;
    listen  [::]:80;
    server_name  51.20.34.227;

    # Serve React.js frontend for root path and specific routes
     location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }


     # Proxy all other routes to Node.js backend
    # location /backend {
    #     rewrite ^/backend(/.*)$ $1 break;  # Remove the '/backend' prefix
    #     proxy_pass http://13.48.48.16:8085;
    #     proxy_http_version 1.1;
    #     proxy_set_header Upgrade $http_upgrade;
    #     proxy_set_header Connection 'upgrade';
    #     proxy_set_header Host $host;
    #     proxy_cache_bypass $http_upgrade;
    # }

    # # Proxy specific routes to Node.js backend
    # location /auth/login {
    #     proxy_pass http://13.48.48.16:8085/auth/login;
    #     # Additional proxy settings if needed
    # }

    # location /auth/register {
    #     proxy_pass http://13.48.48.16:8085/auth/register;
    #     # Additional proxy settings if needed
    # }

    # Handle other requests or static files
    # location /static {
    #     # Configure static file serving settings if needed
    #     root /usr/share/nginx/html/;
    # }

    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
